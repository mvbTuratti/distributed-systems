{
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "serve": "vite preview",
        "python": "VITE_WS_URL=ws://localhost:8000/ws VITE_API_URL=http://localhost:8000/messages/ vite build",
        "elixir": "VITE_WS_URL=ws://localhost:4000/socket VITE_API_URL=http://localhost:4000/messages/ vite build"
    },
    "dependencies": {
        "vue": "^3"
    },
    "devDependencies": {
        "@vitejs/plugin-vue": "^2",
        "@vue/compiler-sfc": "^3",
        "autoprefixer": "^10",
        "daisyui": "^3",
        "postcss": "^8",
        "tailwindcss": "^3",
        "vite": "^2.9.18"
    }
}
